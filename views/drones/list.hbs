<h2>Available drones</h2>
{{#each droneList}}
  <p>{{name}}</p>
  <p>{{propellers}}</p>
  <p>{{maxSpeed}}</p>
  <a href="/drones/{{_id}}/edit">Edit</a>
  <form action="/drones/{{_id}}/delete" method="post"><button>Delete</button></form>
{{/each}}